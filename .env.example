

## 重要   
# 此处可以填多个API_KEY和BASE_URL##
#
# 在aiProviders.ts
# DEFAULT_MODELS中添加模型config
# createProviderById 中ifelse语句，在不同分支下选择不同api_key和base_url
# OpenAI API Key


OPENAI_API_KEY=sk-your-openai-api-key-here
# Qwen / 通义千问 (DashScope OpenAI-compatible endpoint)
# If you want to use Qwen via DashScope compatible mode, set these
QWEN_API_KEY=ak-your-qwen-api-key-here
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1



# MongoDB Connection String
# Get this from MongoDB Atlas (cloud.mongodb.com) or your local MongoDB instance
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/turing-test-game

# Next.js Configuration
NODE_ENV=development

# Pusher (Real-time chat)
# Get these from dashboard.pusher.com
PUSHER_APP_ID=your-pusher-app-id
PUSHER_KEY=your-pusher-key
PUSHER_SECRET=your-pusher-secret
PUSHER_CLUSTER=your-pusher-cluster
NEXT_PUBLIC_PUSHER_KEY=your-pusher-key
NEXT_PUBLIC_PUSHER_CLUSTER=your-pusher-cluster

# Pusher (server)
PUSHER_APP_ID=your_app_id
PUSHER_KEY=your_key
PUSHER_SECRET=your_secret
PUSHER_CLUSTER=your_cluster

# Pusher (client)
NEXT_PUBLIC_PUSHER_KEY=your_key
NEXT_PUBLIC_PUSHER_CLUSTER=your_cluster


# ### 1. 为什么需要两套环境变量？

# 在 Next.js 中，环境变量默认是**仅服务器可见**的。
# *   **服务器端 (Server-side)**：需要完整的权限来“发送”消息和“验证”用户。这需要 `secret`。
# *   **客户端 (Client-side/Browser)**：需要告诉 Pusher 库“连接到哪个应用”，但它**绝对不能**知道 `secret`。如果 `secret` 泄露到浏览器，任何人都可以伪造你的服务器发送虚假消息。

# 这就是为什么我们需要使用 `NEXT_PUBLIC_` 前缀。Next.js 会把带有这个前缀的变量公开给浏览器。

# ---

# ### 2. 你手里的信息该怎么填？

# 你从 Pusher 获得的这一组信息其实是**一套完整的配置**。你只需要把它们映射到不同的变量名即可：

# 请在你的 .env.local 文件中这样填写（用你实际拿到的完整字符串替换）：

# ```dotenv
# # === 服务器端专用 (这些绝对不要公开给前端) ===
# PUSHER_APP_ID="2103224"
# PUSHER_KEY="3b43f68c9**7b71fa42"
# PUSHER_SECRET="4b**5f96f19b"
# PUSHER_CLUSTER="a***p"

# # === 客户端专用 (带有 NEXT_PUBLIC_ 前缀) ===
# # 注意：这部分的值其实是从上面重复过来的，但前缀允许浏览器读取它们
# NEXT_PUBLIC_PUSHER_KEY="3b43f68c9**7b71fa42"
# NEXT_PUBLIC_PUSHER_CLUSTER="a***p"
# ```

# ### 总结归属：
# *   **`secret`**: **仅服务器**。它就像是数据库密码。
# *   **`app_id`**: **仅服务器**。
# *   **`key`**: **双方共有**。它是你的公开标识符。
# *   **`cluster`**: **双方共有**。

# ### 验证步骤 🚀
# 1.  保存 .env.local。
# 2.  重启你的开发服务器 (`npm run dev`)。
# 3.  现在你应用里的 `pusher-js` (前端) 就能看到 `key` 连接到 Pusher，而 `pusher` (后端 API) 就能用 `secret` 安全地推送消息了。